# ※ 옛 시나리오에서 옮겨 온 프리셋이다 — 판정(checks)·회귀 허용오차는
#   더 이상 YAML 에 없다(잰 값을 사람이·클로드가 읽는다).
#   프리셋이 담는 것은 ★재생 조건★ 뿐이다: 영상·구간·모드·파라미터.
# 차선 알고리즘(perception + camera_judgment) 시험 — 실차 야간 영상 B
#   lane_night_a.yaml 과 같은 기준. 영상만 다르다(night_a 와 다른 구간·주행).

name: lane_night_b
contract: contracts/white_camera.yaml
video: /home/mad2/video/night_b.mp4
mode: lockstep
start: 0
limit: 0
stride: 1
warmup_s: 8.0
sync_timeout: 4.0
discard_first: 5
params:
  perception: {show_window: false, record_video: false, enable_undistort: true}
  camera_judgment: {}
