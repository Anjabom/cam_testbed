# ※ 옛 시나리오에서 옮겨 온 프리셋이다 — 판정(checks)·회귀 허용오차는
#   더 이상 YAML 에 없다(잰 값을 사람이·클로드가 읽는다).
#   프리셋이 담는 것은 ★재생 조건★ 뿐이다: 영상·구간·모드·파라미터.
# black_ws lane_vote 이식 검증 — 실차 야간 영상 B
#   ★vote_night_b.yaml 과 같은 영상·같은 불변식★ 을 쓴다. 계약만 black_vote 다.
#   그래야 두 리포트를 나란히 놓고 "이식하다 깨먹지 않았다"를 볼 수 있다.
#
#   ⚠️ 이 영상은 ★자작차★ 로 찍은 것이다. 1/5카 영상은 아직 없다.
#      따라서 이 프리셋가 재는 것은 ★이식 무결성★ 이지 1/5카 성능이 아니다.
#      1/5카 캘리브가 끝나면 새 영상으로 프리셋를 하나 더 만든다.
#
#   ⚠️ 대조군 변형(defer_off)은 뺐다 — 이식 검증에는 base 만 있으면 되고,
#      1292프레임 YOLO 추론을 두 번 돌 이유가 없다. 알고리즘 튜닝으로 넘어가면
#      vote_night_b.yaml 의 variants 절을 그대로 가져오면 된다.

name: black_vote_night_b
contract: contracts/black_vote.yaml
video: /home/mad2/video/night_b.mp4
mode: lockstep
start: 0
limit: 0
stride: 1
warmup_s: 8.0
sync_timeout: 4.0
discard_first: 5
params:
  perception: {show_window: false, record_video: false, enable_undistort: true}
  camera_judgment: {}
